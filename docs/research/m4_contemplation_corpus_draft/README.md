# M4 Contemplation Corpus Draft

This directory preserves the generated `PromptFace(...)` fragments used
to build the ContemplationCorpus defaults tracked by issue #84.

The files intentionally use the `.pyfrag` extension because they are
source fragments, not standalone Python modules. The importable runtime
surface lives in `clanker_soul.pulse.contemplation_defaults`.
